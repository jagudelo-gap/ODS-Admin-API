meta {
  name: OdsInstanceDerivatives by ID - Not Found
  type: http
  seq: 15
}

get {
  url: {{API_URL}}/v2/OdsInstanceDerivatives/{{NotExistOdsInstancesDerivativeId}}
  body: none
  auth: inherit
}

script:post-response {
  test("GET OdsInstanceDerivatives NotFound: Status code is Not Found", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
  test("GET OdsInstanceDerivatives NotFound: Response matches error format", function () {
      const response = res.getBody();
  
      expect(response).to.have.property("title");
  });
  
  test("GET OdsInstanceDerivatives NotFound: Response title is helpful and accurate", function () {
      const response = res.getBody();
  
      expect(response.title).to.contain("Not found");
      expect(response.title).to.contain("odsInstanceDerivative");
      expect(response.title).to.contain(bru.getEnvVar("NotExistOdsInstancesDerivativeId"));
  });
}

settings {
  encodeUrl: true
}
