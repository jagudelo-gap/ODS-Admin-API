meta {
  name: ResourceClaimsActions - Filter by non-existing ResourceName
  type: http
  seq: 7
}

get {
  url: {{API_URL}}/v2/resourceClaimActions?resourceName=1234
  body: none
  auth: inherit
}

params:query {
  resourceName: 1234
}

script:post-response {
  test("GET ResourceClaimsActions: Status code is OK", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
  const response = res.getBody();
  const Ajv = require('ajv');
  const ajv = new Ajv();
  const GetResourceClaimsActionsSchema = {
    "type": "array",
    "items": [
      {
        "type": "object",
        "properties": {
          "resourceClaimId": {
            "type": "integer"
          },
          "resourceName": {
            "type": "string"
          },
          "claimName": {
            "type": "string"
          },
          "actions": {
            "type": "array",
            "items": [
            {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                }
              },
              "required": [
                "name"
              ]
            }]
          }
        },
        "required": [
          "resourceClaimId",
          "resourceName",
          "claimName",
          "actions"
        ]
      }
    ]
  }
  
  test("GET ResourceClaimsActions: Validation Schema Response", () => {
      const valid = ajv.validate(GetResourceClaimsActionsSchema, response);
      expect(valid).to.be.true;
      
      if (!valid) {
          console.log("Schema validation errors:", ajv.errors);
      }
  });
  
  test("GET ResourceClaimsActions: Response result not includes resource claim action", function () {
      const results = res.getBody();
      expect(results).to.have.lengthOf(0);
  });
  
}

settings {
  encodeUrl: true
}
