meta {
  name: Profiles
  type: http
  seq: 22
}

delete {
  url: {{API_URL}}/v2/profiles/{{CreatedProfileId}}
  body: none
  auth: inherit
}

script:post-response {
  test("DELETE Profile: Status code is OK", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
}
